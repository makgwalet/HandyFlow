@ApplicationModule(allowedDependencies = {"shared", "notifications", "hr", "evidence",  "accounting"})
package za.co.handyflow.platform.expenses;

import org.springframework.modulith.ApplicationModule;