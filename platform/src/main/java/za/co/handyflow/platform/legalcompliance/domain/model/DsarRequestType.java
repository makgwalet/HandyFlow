package za.co.handyflow.platform.legalcompliance.domain.model;

/** POPIA data-subject rights this org-wide register tracks requests for (Sections 23-25). */
public enum DsarRequestType {
    ACCESS,
    CORRECTION,
    DELETION,
    OBJECTION
}
