-- Placeholder version — confirm real next version before applying.
-- UNVERIFIED table name — inferred as pay_bureau_profiles from naming
-- convention, not confirmed against PayBureauProfile's real @Table
-- annotation. Check before running.
ALTER TABLE pay_bureau_profiles ADD COLUMN logo_evidence_id UUID;